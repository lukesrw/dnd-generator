{
    "main": "./index",
    "scripts": {
        "watch-end": "node clean",
        "build-ts": "tsc",
        "build-sass": "sass ./css:./css",
        "build-tailwind": "tailwindcss-cli build ./css/style.css -o ./css/style.css",
        "build": "npm install && npm run watch-end && npm run build-ts",
        "watch-start": "npm run build",
        "watch-after-start": "node index",
        "test": "npm run build && mocha test --reporter min"
    },
    "devDependencies": {},
    "dependencies": {
        "@types/node": "^14.14.37"
    }
}
